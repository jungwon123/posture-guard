# HARNESS.md — 스킬·훅·서브에이전트 설계

> CLAUDE.md(진입점·라우팅·문서 동기화)를 **보완**합니다.
> CLAUDE.md가 "어디로 갈지(라우팅)"라면, 이 문서는 **스킬·훅·서브에이전트**를 어디에 배치할지 정의합니다.
> 원칙: **각 레이어는 서로 다른 일을 하고, 겹치지 않는다.**

---

## 0. 4-레이어 역할 분담

| 레이어 | 로딩 시점 | 성격 | 한 줄 역할 |
|--------|----------|------|-----------|
| CLAUDE.md | 항상 로드 | 강함 | 진입점·라우팅·프로젝트 규칙 |
| Skill | 호출 시 로드 | 확률론(판단) | "그 작업을 **어떻게** 하는가" — 반복 절차 |
| Hook | 이벤트 시 실행 | 결정론(강제) | "무엇이 **반드시** 일어나야 하는가" |
| Subagent | 위임 시 격리 | 격리 | 별도 컨텍스트가 필요한 곁가지 |

핵심 대비:
- **Hook = 결정론적.** 시스템이 강제 실행. Claude는 훅 발동을 인지하지 못함.
- **Skill = 확률론적.** Claude가 맥락을 보고 스스로 호출. `description`이 트리거.
- **Subagent = 격리.** 별도 컨텍스트 창. 중간 결과를 본문에 남기지 않고 요약만 반환.

---

## 1. 배치 결정 트리 — *설계 시점*

> "이 작업을 **어떤 파일로 만들어 둘까?**" 위에서부터 순서대로. 먼저 걸리는 곳이 정답.

1. **100% 강제인가?** (판단 여지 없음) → **Hook** (`.claude/settings.json`)
2. **반복 절차인가?** (매번 조금씩 다름) → **Skill** (`.claude/skills/`)
3. **격리가 필요한가?** (중간 결과를 본문에 안 남기고 싶음) → **Subagent** (`.claude/agents/`)
4. **전역 규칙·라우팅인가?** → **CLAUDE.md**

### Skill·Subagent 둘 다 걸칠 때
- **중간 과정을 보며 조종하고 싶다** → Skill (본문에서 단계별 진행)
- **결과만 받고 싶다** → Subagent (별도 창, 요약만 반환)
- 예) "임계값 튜닝": 녹화→리플레이→조정을 단계별로 보려면 Skill / 녹화 여러 개를 한꺼번에 돌려 최적 조합 요약만 받으려면 Subagent

---

## 2. Hook 설계 (강제 — `.claude/settings.json`)

> 비협상 규칙만. exit 0 = 진행, exit 2 = 차단(stderr가 Claude에 피드백).

| 우선 | 이벤트 | 훅 | 동작 | 상태 |
|------|--------|-----|------|------|
| 1 | PreToolUse (Bash: git commit/add) | 시크릿·개인데이터 차단 | `.env`·키 파일에 더해 **`posture_profile.json`·`posture.db`·`posture_rec_*.json`**(개인 신체 기준·녹화 신호) 스테이징 시 exit 2 | ✅ 구현됨 (`.claude/hooks/block-secrets.sh`) |
| 2 | PostToolUse (Edit/Write: *.py) | 문법 게이트 | `python3 -m py_compile` → 에러 시 피드백 | 나중에 (필요해지면) |
| 3 | Stop | 판정 회귀 체크 | 기준 녹화 JSON `--replay` 실행 → 전이 결과 비교 | 나중에 (기준 녹화 확보 후) |

**원칙**
- 훅은 **보이지 않게 실행**. 차단(exit 2)은 **하드 개런티가 필요한 곳에만.**
- **시작점은 시크릿 차단 훅 1개.** 이 프로젝트에선 개인 신체 데이터(profile/db/녹화)가 곧 시크릿이다.

---

## 3. Skill 설계 (절차 — `.claude/skills/`)

> 반복하지만 매번 조금씩 다른 작업. **아직 만들지 않음** — 실제로 2회 이상 반복될 때 도입.

| 스킬 (후보) | 트리거 키워드 | 담는 내용 |
|------|-------------|-----------|
| threshold-tuning | 임계값, 튜닝, 민감도, 오탐 | 녹화(`r`) → `--replay` → 튜닝 블록 조정 → 재검증 루프 절차. 어떤 증상에 어떤 파라미터를 만지는지 매핑 (오탐↑→`DEADZONE_Z`/`BAD_ENTER_SUSTAIN`, 둔감→`WEIGHTS`) |
| signal-add | 새 신호, 랜드마크 추가 | 신호 추가 체크리스트: `extract_signals` → `WEIGHTS`·`BAD_DIRECTION` 등록 → 정규화(비율) 확인 → 캘리브레이션 재수행 → `features/signals.md` 갱신 |

**원칙**
- `description`이 **모호하면 트리거 실패**. "품질 체크" ✗ → "리플레이 기반 임계값 튜닝" ✓
- skill.md 본문은 린하게. 예시·엣지케이스는 별도 파일 → 필요할 때만 로드.
- 도입 시 CLAUDE.md 라우팅 테이블의 **"연결 스킬" 칼럼**에 매핑을 적을 것.

---

## 4. Subagent 설계 (격리 — `.claude/agents/`)

> 본문을 어지럽히는 큰 작업. **아직 만들지 않음** — 본문이 실제로 어지러워질 때 도입.

| 서브에이전트 (후보) | 용도 | 왜 격리하나 |
|-------------|------|------------|
| recording-analysis | 녹화 JSON 여러 개를 리플레이·통계 분석 | 프레임 단위 중간 출력이 길고 한 번 보면 안 봄 |
| mediapipe-research | Face Landmarker 변환행렬 업그레이드(ADR-0001 재검토) 조사 | 외부 문서 조사라 본문 맥락과 무관 |

---

## 5. 통합 실행 흐름 — *실행 시점*

```
요청 들어옴
  ① CLAUDE.md 라우팅 → 작업 식별 · 문서 좁히기 (depth 2)
  ② Skill (있으면) → 그 작업의 절차 제공
  ③ 작업 수행 → 최소 컨텍스트로
       └ (무거운 곁가지) → Subagent 격리 · 요약 반환
  ④ Hook (강제) → 커밋 시 시크릿·개인데이터 차단
       └ (위반 시) → exit 2 차단 · 피드백 → 수정
  ⑤ 문서 동기화 → 읽은 문서 점검 · 자동 갱신 (CLAUDE.md 5절)
  ⑥ 응답 + [문서 동기화] 변경 내역 명시
```

---

## 6. 안티패턴

- ❌ 반복 절차를 CLAUDE.md에 다 적기 → 항상 로드되어 컨텍스트 비대. **스킬로 분리**
- ❌ 판단이 필요한 걸 훅으로 강제 → 훅은 결정론적인 것만
- ❌ 100% 보장이 필요한 걸 스킬로 → 하드 개런티는 훅(exit 2)
- ❌ 스킬 description을 모호하게 → 자동 발동 실패
- ❌ 가벼운 작업을 서브에이전트로 → 격리 비용만 듦
- ❌ 한 번에 스킬·에이전트 다 만들기 → 훅 1개부터, 반복이 확인될 때 늘림
