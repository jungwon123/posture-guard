# LiveKit 셀프호스트 설정 (비밀키 없음 — 키는 compose가 LIVEKIT_KEYS 환경변수로 주입)
# 기존 /opt/livekit/config.yaml(네이티브)과 동일 포트 구성.
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50200
  use_external_ip: true
logging:
  level: info
